module.exports = function(grunt) {
  grunt.registerTask('build_vagrant', 'Setting up Vagrant and then building the server', function() {
    grunt.log.writeln("Vagrant would be configured, then notice to `vagrant up`");
  });
};